export type Post = {
    title: string;
    preview: string;
    image_url: string;
    author: string;
    create_date: string;
    content: string;
    slug: string;
    category: string;
};

export type Profile = {
    name: string;
    preview: string;
    image_url: string;
    job: string;
    github_url: string;
    content: string;
    slug: string;
};
